# Gemini API key. If unset, agents fall back to deterministic
# local reasoning (see app/utils/gemini_client.py) — the system
# still runs without this, just with lower-quality reasoning.
GEMINI_API_KEY=

# Comma-separated list of frontend origins allowed to call this API.
ALLOWED_ORIGINS=http://localhost:5173
